name: Build Pumpkin Android APK

on:
  workflow_dispatch:
    inputs:
      pumpkin_ref:
        description: 'Pumpkin branch / tag / commit (leave blank for master)'
        required: false
        default: 'master'

  schedule:
    - cron: '0 2 * * *'

  push:
    branches: [ main, master ]
    paths:
      - 'app/**'
      - '.github/workflows/**'

env:
  PUMPKIN_REPO: https://github.com/Pumpkin-MC/Pumpkin.git
  MIN_SDK: 26
  NDK_VERSION: '26.3.11579264'
  JAVA_VERSION: '17'

# -----------------------------------------------------------------------
# Job 1 – Cross-compile Pumpkin (arm64 and x86_64 run in parallel)
# -----------------------------------------------------------------------
jobs:
  build-pumpkin:
    name: Cross-compile Pumpkin (${{ matrix.abi }})
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        include:
          - abi: arm64-v8a
            rust_target: aarch64-linux-android
          - abi: x86_64
            rust_target: x86_64-linux-android

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---- Java 17 must come BEFORE setup-android (sdkmanager requires JDK 17+) ----
      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin

      # ---- Android SDK + NDK ----
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK
        run: |
          sdkmanager "ndk;${{ env.NDK_VERSION }}"
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/${{ env.NDK_VERSION }}" >> $GITHUB_ENV

      # ---- Rust ----
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      # ---- Pumpkin source (submodules required for pumpkin-plugin-wit) ----
      # Cloned BEFORE the cache-restore step so that hashFiles() below can
      # actually see Pumpkin/Cargo.lock, and so this step's "rm -rf Pumpkin"
      # never runs after (and therefore never deletes) a restored cache.
      - name: Clone Pumpkin
        run: |
          REF="${{ github.event.inputs.pumpkin_ref || 'master' }}"
          rm -rf Pumpkin
          if ! git clone --recurse-submodules --depth=1 --shallow-submodules --branch "$REF" ${{ env.PUMPKIN_REPO }} Pumpkin; then
            echo "Branch/tag '$REF' not found, falling back to default branch"
            rm -rf Pumpkin
            git clone --recurse-submodules --depth=1 --shallow-submodules ${{ env.PUMPKIN_REPO }} Pumpkin
          fi
          echo "PUMPKIN_COMMIT=$(git -C Pumpkin rev-parse --short HEAD)" >> $GITHUB_ENV

      # Build output is written OUTSIDE the Pumpkin/ directory so that
      # re-cloning Pumpkin (rm -rf Pumpkin above) never touches the cache.
      - name: Set CARGO_TARGET_DIR
        run: echo "CARGO_TARGET_DIR=${{ github.workspace }}/cargo-target" >> $GITHUB_ENV

      # ---- Cargo cache (per target, keyed on the actual Cargo.lock) ----
      - name: Restore Cargo cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            cargo-target/
          key: cargo-${{ matrix.rust_target }}-${{ hashFiles('Pumpkin/Cargo.lock') }}
          restore-keys: |
            cargo-${{ matrix.rust_target }}-

      # ---- cargo-ndk cache ----
      - name: Restore cargo-ndk cache
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin/cargo-ndk
          key: cargo-ndk-${{ runner.os }}-v3

      - name: Install cargo-ndk
        run: |
          if ! command -v cargo-ndk &>/dev/null; then
            cargo install cargo-ndk --version "^3"
          else
            echo "cargo-ndk already cached"
          fi

      # ---- Build ----
      - name: Build Pumpkin for ${{ matrix.abi }}
        working-directory: Pumpkin
        run: |
          cargo ndk \
            --target ${{ matrix.rust_target }} \
            --platform ${{ env.MIN_SDK }} \
            build --release -p pumpkin

      # ---- Sanity check ----
      - name: Verify binary
        run: |
          BINARY="cargo-target/${{ matrix.rust_target }}/release/pumpkin"
          ls -lh "$BINARY"
          file "$BINARY"

      # ---- Pass binary to the next job ----
      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: pumpkin-binary-${{ matrix.abi }}
          path: cargo-target/${{ matrix.rust_target }}/release/pumpkin
          retention-days: 1

# -----------------------------------------------------------------------
# Job 2 – Build the Android APK
# -----------------------------------------------------------------------
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-22.04
    needs: build-pumpkin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---- Java 17 must come BEFORE setup-android ----
      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # ---- Place binaries as native "libraries" so Android extracts them
      #      as executable files (files dir copies are blocked by W^X) ----
      - name: Download arm64-v8a binary
        uses: actions/download-artifact@v4
        with:
          name: pumpkin-binary-arm64-v8a
          path: app/src/main/jniLibs/arm64-v8a/

      - name: Rename and mark arm64-v8a binary executable
        run: |
          mv app/src/main/jniLibs/arm64-v8a/pumpkin app/src/main/jniLibs/arm64-v8a/libpumpkin.so
          chmod +x app/src/main/jniLibs/arm64-v8a/libpumpkin.so

      - name: Download x86_64 binary
        uses: actions/download-artifact@v4
        with:
          name: pumpkin-binary-x86_64
          path: app/src/main/jniLibs/x86_64/

      - name: Rename and mark x86_64 binary executable
        run: |
          mv app/src/main/jniLibs/x86_64/pumpkin app/src/main/jniLibs/x86_64/libpumpkin.so
          chmod +x app/src/main/jniLibs/x86_64/libpumpkin.so

      # ---- Verify layout ----
      - name: Show jniLibs layout
        run: |
          echo "=== jniLibs/ ==="
          find app/src/main/jniLibs -type f | while read f; do
            echo "  $f  ($(du -sh "$f" | cut -f1))"
          done

      # ---- Ensure gradlew is executable ----
      - name: Make gradlew executable
        run: chmod +x gradlew

      # ---- Gradle cache ----
      - name: Restore Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches/
            ~/.gradle/wrapper/
          key: gradle-${{ hashFiles('**/*.gradle', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-

      # ---- Build APKs ----
      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Build Release APK (unsigned)
        run: ./gradlew assembleRelease --no-daemon

      # ---- Summary ----
      - name: Show APK sizes
        run: |
          echo "=== Build outputs ==="
          find app/build/outputs/apk -name "*.apk" | while read apk; do
            echo "  $apk  ($(du -sh "$apk" | cut -f1))"
          done

      # ---- Upload APKs ----
      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: PumpkinAndroid-APK
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            app/build/outputs/apk/release/app-release-unsigned.apk
          retention-days: 30

      # ---- Publish to GitHub Releases on tag push ----
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app/build/outputs/apk/debug/app-debug.apk
            app/build/outputs/apk/release/app-release-unsigned.apk
          body: |
            ## 🎃 Pumpkin Android

            Run a Pumpkin Minecraft server on Android.

            ### Installation
            1. Download `app-debug.apk`
            2. Enable *Install from unknown sources* in Android settings
            3. Install the APK

            ### Requirements
            - Android 8.0 (API 26) or higher
            - arm64-v8a (physical devices) / x86_64 (emulator)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
